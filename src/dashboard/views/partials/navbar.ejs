<nav class="navbar">
    <div class="nav-brand">
        <span class="logo">🤖</span>
        <span class="brand-text">Nerox Dashboard</span>
    </div>
    <div class="nav-links">
        <% if (role === 'admin') { %>
            <a href="/admin" class="nav-link">📊 Dashboard</a>
            <a href="/admin/bulk" class="nav-link">📦 Bulk Ops</a>
        <% } else { %>
            <a href="/user" class="nav-link">📊 Dashboard</a>
            <a href="/user/search" class="nav-link">🔍 Search</a>
        <% } %>
    </div>
    <div class="nav-user">
        <span class="user-badge <%= role %>"><%= role === 'admin' ? '🛡️' : '👤' %> <%= username %></span>
        <a href="/logout" class="btn btn-logout">Logout</a>
    </div>
</nav>
